<!--pages/subject/subject.wxml-->
<view class='wrap' wx:for="{{list}}" wx:key="{{index}}" data-type="{{item.type}}" bindtap='tap'>
  <image class='image' mode='widthFix' src='{{item.url}}'></image>
  <text class='title'>{{item.type}}</text>
</view>
